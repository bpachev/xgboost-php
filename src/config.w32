// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("xgboost", "for xgboost support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("xgboost", "enable xgboost support", "no");

if (PHP_XGBOOST != "no") {
	EXTENSION("xgboost", "xgboost.cpp");
}

